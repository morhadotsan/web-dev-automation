<?php
// functions/blogRedirects.php — per-slug 301 redirects for renamed posts.
// Included from blogs_on/blog_details.php BEFORE the SELECT for the article row.
// Leave empty unless the project actually needs slug migrations.

/*
// Usage example:
if($get_id == "old-slug"){
    header("Location: ".$wiscoy_url."blogs_on/new-slug", true, 301);
    exit();
}
*/
?>
