<!-- includes/footer.php — site footer.
     IMPORTANT: The HTML structure here MUST be taken from the input template's footer
     region. Only add PHP globals. Never synthesize elements (Follow-Us grids, social
     icon rows, elaborate copyright blocks) not present in the input HTML.

     Globals available: $wiscoy_url, $shortTitle, $adm_facebook, $adm_twitter,
                        $adm_instagram, $adm_linkedin, $adm_whatsapp, $adm_email.
-->

<!-- Adapt class names and structure from the input template's footer. -->
<div class="{{FOOTER_CLASS}}">
    <div class="{{COPYRIGHT_CLASS}}">
        <?= $shortTitle; ?> &copy; <script type="text/javascript">document.write(new Date().getFullYear())</script>. All Rights Reserved.
    </div>
    <!-- Back-to-top: href target must match the id of the input template's top-level wrapper. -->
    <a href="#{{TOP_WRAPPER_ID}}" class="{{BACK_TO_TOP_CLASS}}">
        <img src="<?= $wiscoy_url; ?>assets/images/back-top.png" loading="lazy" alt="Back to top" class="{{UP_ICON_CLASS}}" />
    </a>
</div>
