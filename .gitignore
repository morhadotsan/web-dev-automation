# Secrets — never commit
.env
.env.*
!.env.example

# Generated output (one folder per finished project)
output/

# Drop-zone for new input projects (don't pollute history with raw inputs)
input/

# Test artifacts
tests/screenshots/
tests/diff/
tests/results.json

# Node / Puppeteer (used by tests/visual_diff.js)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# PHP
vendor/
composer.lock

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Logs
*.log
